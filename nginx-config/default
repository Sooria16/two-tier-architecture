server {
    listen 80;

    # Serve frontend
    root /var/www/html;
    index index.html;

    # Forward API requests to backend
    location /api/ {
        proxy_pass http://54.190.93.13:3000/;
        proxy_set_header Host $host;
    }
}
